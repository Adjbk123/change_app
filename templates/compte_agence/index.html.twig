{% extends 'base.html.twig' %}

{% block title %}CompteAgence index{% endblock %}

{% block body %}
    <h1>CompteAgence index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>SoldeInitial</th>
                <th>SoldeRestant</th>
                <th>SueilAlerte</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for compte_agence in compte_agences %}
            <tr>
                <td>{{ compte_agence.id }}</td>
                <td>{{ compte_agence.soldeInitial }}</td>
                <td>{{ compte_agence.soldeRestant }}</td>
                <td>{{ compte_agence.sueilAlerte }}</td>
                <td>
                    <a href="{{ path('app_compte_agence_show', {'id': compte_agence.id}) }}">show</a>
                    <a href="{{ path('app_compte_agence_edit', {'id': compte_agence.id}) }}">edit</a>
                </td>
            </tr>

        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_compte_agence_new') }}">Create new</a>
{% endblock %}
