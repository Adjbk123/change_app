{% extends 'base.html.twig' %}

{% block title %}AffectationCaisse{% endblock %}

{% block body %}
    <h1>AffectationCaisse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ affectation_caisse.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ affectation_caisse.date ? affectation_caisse.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>IsActive</th>
                <td>{{ affectation_caisse.isActive ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_affectation_caisse_index') }}">back to list</a>

    <a href="{{ path('app_affectation_caisse_edit', {'id': affectation_caisse.id}) }}">edit</a>

    {{ include('affectation_caisse/_delete_form.html.twig') }}
{% endblock %}
