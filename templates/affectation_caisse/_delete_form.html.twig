<form method="post" action="{{ path('app_affectation_caisse_delete', {'id': affectation_caisse.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ affectation_caisse.id) }}">
    <button class="btn">Delete</button>
</form>
