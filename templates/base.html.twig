<!doctype html>
<html lang="fr">


<head>

    <meta charset="utf-8" />
    <title>{% block title %}MySwap{% endblock %} | MySwap - Plateforme de Change Intelligente</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="MySwap est une application web professionnelle de gestion de change, développée pour les agences et opérateurs de change au Bénin. Optimisez vos opérations de change avec précision et efficacité.">
    <meta name="author" content="SECS SARL" />
    <meta name="keywords" content="change, devise, agence de change, XOF, USD, EUR, NGN, fintech, Bénin, caisse, transfert, myswap" />
    <meta name="robots" content="index, follow" />

    <!-- Favicon -->
    <link rel="shortcut icon" href="{{ asset('images/mini-logo-myswap.png') }}">


    {{ include('partials/head-css.html.twig') }}
    {% block stylesheets %}

        {# Pas de script JavaScript spécifique pour cette vue, sauf si vous avez des fonctionnalités interactives supplémentaires #}
    {% endblock %}


    <style>
        .progress-container {
            display: flex;
            justify-content: center;
            align-items: center;
            margin-bottom: 2rem;
            position: relative;
        }

        .progress-step {
            background-color: #e0e0e0;
            color: #6c757d;
            border-radius: 20px;
            padding: 8px 15px;
            font-weight: 700;
            transition: background-color .3s ease, color .3s ease;
            z-index: 1;
            white-space: nowrap;
        }

        .progress-step.active {
            background-color: #0d6efd;
            color: #fff;
        }

        .progress-line {
            flex-grow: 1;
            height: 4px;
            background-color: #e0e0e0;
            position: absolute;
            width: calc(100% - 350px);
            z-index: 0;
        }

        .form-step.active {
            display: block;
        }

        .form-step {
            display: none;
        }

        .is-invalid {
            border-color: #dc3545;
        }

        .is-invalid:focus {
            box-shadow: 0 0 0 .25rem rgba(220, 53, 69, .25);
        }

    </style>

</head>

{{ include('partials/body.html.twig') }}

<!-- Begin page -->
<div id="layout-wrapper">

    {{ include('partials/menu.html.twig') }}

    <!-- ============================================================== -->
    <!-- Start right Content here -->
    <!-- ============================================================== -->
    <div class="main-content">

        <div class="page-content">
            <div class="container-fluid">

                <!-- start page title -->
                <div class="row">
                    <div class="col-12">
                        <div
                            class="page-title-box d-sm-flex align-items-center justify-content-between">
                            <h4 class="mb-sm-0 font-size-18">

                                {{ block('title') }}
                            </h4>

                            <div class="page-title-right">
                                <ol class="breadcrumb m-0">
                                    <li class="breadcrumb-item"><a
                                            href="javascript: void(0);">
                                            {% block breadcrumd %}
                                                Tableau de bord
                                            {% endblock %}
                                        </a></li>
                                    <li class="breadcrumb-item active">
                                        {% block breadcrumdActive %}
                                            Main
                                        {% endblock %}
                                    </li>
                                </ol>
                            </div>

                        </div>
                    </div>
                </div>

                <div class="row">


                    <div class="col-md-12">
                        {% for label, messages in app.flashes %}
                            {% for message in messages %}
                                <div class="alert alert-{{ label }} my-3">
                                    {{ message }}
                                </div>
                            {% endfor %}
                        {% endfor %}
                        {% if app.current_route !="app_dashboard" %}
                        <div class="card">
                            <div class="card-body">
                        {% endif %}
                                <div class="col-md-12">
                                    <div class="text-end my-3">
                                        {% block button %}

                                        {% endblock %}
                                    </div>
                                </div>
                                {% block body %}

                                {% endblock %}
                                {% if app.current_route !="app_dashboard" %}
                                    </div>
                        </div>
                                        {% endif %}

                    </div>

                </div>

            </div>
            <!-- container-fluid -->
        </div>
        <!-- End Page-content -->


        <footer class="footer">
            <div class="container-fluid">
                <div class="row">
                    <div class="col-sm-6">
                        {{ date()|date('Y') }} © MoneyChange.
                    </div>
                    <div class="col-sm-6">
                        <div class="text-sm-end d-none d-sm-block">
                            Développé par SECS SARL
                        </div>
                    </div>
                </div>
            </div>
        </footer>
    </div>
    <!-- end main content-->

</div>
<!-- END layout-wrapper -->
{{ include('partials/right-sidebar.html.twig') }}

        {{ include('partials/vendor-scripts.html.twig') }}



{% block javascripts %}

    {# Pas de script JavaScript spécifique pour cette vue, sauf si vous avez des fonctionnalités interactives supplémentaires #}
{% endblock %}
</body>

</html>
