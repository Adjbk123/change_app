{% extends 'base.html.twig' %}

{% block title %}CompteClient index{% endblock %}

{% block body %}
    <h1>CompteClient index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>SoldeInitial</th>
                <th>SoldeActuel</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for compte_client in compte_clients %}
            <tr>
                <td>{{ compte_client.id }}</td>
                <td>{{ compte_client.soldeInitial }}</td>
                <td>{{ compte_client.soldeActuel }}</td>
                <td>
                    <a href="{{ path('app_compte_client_show', {'id': compte_client.id}) }}">show</a>
                    <a href="{{ path('app_compte_client_edit', {'id': compte_client.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_compte_client_new') }}">Create new</a>
{% endblock %}
