{% extends 'base.html.twig' %}

{% block title %}CompteClient{% endblock %}

{% block body %}
    <h1>CompteClient</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ compte_client.id }}</td>
            </tr>
            <tr>
                <th>SoldeInitial</th>
                <td>{{ compte_client.soldeInitial }}</td>
            </tr>
            <tr>
                <th>SoldeActuel</th>
                <td>{{ compte_client.soldeActuel }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_compte_client_index') }}">back to list</a>

    <a href="{{ path('app_compte_client_edit', {'id': compte_client.id}) }}">edit</a>

    {{ include('compte_client/_delete_form.html.twig') }}
{% endblock %}
