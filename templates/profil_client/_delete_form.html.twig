<form method="post" action="{{ path('app_profil_client_delete', {'id': profil_client.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ profil_client.id) }}">
    <button class="btn">Delete</button>
</form>
