<form method="post" action="{{ path('app_piece_identite_delete', {'id': piece_identite.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ piece_identite.id) }}">
    <button class="btn">Delete</button>
</form>
