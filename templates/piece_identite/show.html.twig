{% extends 'base.html.twig' %}

{% block title %}PieceIdentite{% endblock %}

{% block body %}
    <h1>PieceIdentite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ piece_identite.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ piece_identite.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_piece_identite_index') }}">back to list</a>

    <a href="{{ path('app_piece_identite_edit', {'id': piece_identite.id}) }}">edit</a>

    {{ include('piece_identite/_delete_form.html.twig') }}
{% endblock %}
