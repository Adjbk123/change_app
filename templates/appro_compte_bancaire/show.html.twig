{% extends 'base.html.twig' %}

{% block title %}ApproCompteBancaire{% endblock %}

{% block body %}
    <h1>ApproCompteBancaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ appro_compte_bancaire.id }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ appro_compte_bancaire.montant }}</td>
            </tr>
            <tr>
                <th>DateAppro</th>
                <td>{{ appro_compte_bancaire.dateAppro ? appro_compte_bancaire.dateAppro|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_appro_compte_bancaire_index') }}">back to list</a>

    <a href="{{ path('app_appro_compte_bancaire_edit', {'id': appro_compte_bancaire.id}) }}">edit</a>

    {{ include('appro_compte_bancaire/_delete_form.html.twig') }}
{% endblock %}
