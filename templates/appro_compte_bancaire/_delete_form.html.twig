<form method="post" action="{{ path('app_appro_compte_bancaire_delete', {'id': appro_compte_bancaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ appro_compte_bancaire.id) }}">
    <button class="btn">Delete</button>
</form>
